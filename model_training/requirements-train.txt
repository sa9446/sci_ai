# Training-only dependencies. Kept separate from the main sci_ai_engine
# requirements.txt so this pipeline never needs anthropic/pydantic — it has
# no dependency on the Claude-backed sci_engine package at all.
torch>=2.3.0
tokenizers>=0.19.0
numpy>=1.26.4
tqdm>=4.66.0
requests>=2.32.0
